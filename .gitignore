# The user's private overlay and the script's own state: permissions, notes, drafts,
# last-seen markers. They live beside the script when the clone is synced between
# machines, and must never reach the repository
user/
state/
falsify.out/
.DS_Store
